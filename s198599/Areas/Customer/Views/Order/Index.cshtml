@model IEnumerable<BOL.Models.Order>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DateTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OrderPriceTotal)
            
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DateTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OrderPriceTotal)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OrderNumber }) |
            @Html.ActionLink("Details", "Details", new { id=item.OrderNumber }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OrderNumber })
        </td>
    </tr>
}

</table>
